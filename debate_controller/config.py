"""프로젝트 전역 설정값"""

# ── LLM 설정 ──────────────────────────────────────────────
MODEL_NAME = "qwen3.5:9b"
TEMPERATURE = 0.7           # 토론의 다양성을 위해 적절한 창의성 부여
MAX_RESPONSE_TOKENS = 1536  # 응답 최대 토큰 수 대폭 증가 (글 짤림 완벽 방지)

# ── 토론 설정 ─────────────────────────────────────────────
MAX_TURNS = 4               # 9B 모델 한계 고려: 4턴 내에 승부를 보는 '핵심 숏폼 토론'으로 변경
JUDGE_INTERVAL = 4          # 중재자 개입 주기 (마지막 턴 종료 후 최종 심사역할 수행)

# ── 컨텍스트 관리 ─────────────────────────────────────────
CONTEXT_WINDOW_SIZE = 4096  # 11GB VRAM 안정 운용 (4-bit 양자화 기준)
SLIDING_WINDOW_KEEP = 8     # 4턴 토론이므로 윈도우 축쇠 불필요 (안정적 컨텍스트 유지)
USE_SUMMARY = False         # True: 요약 후 트리밍, False: 단순 잘라내기
REPETITION_PENALTY = 1.1    # 과도한 페널티(1.25) 역효과 방지를 위해 기본값(1.1) 수준으로 완화

# ── 에러 처리 ─────────────────────────────────────────────
MAX_RETRIES = 3             # LLM 호출 실패 시 최대 재시도 횟수
